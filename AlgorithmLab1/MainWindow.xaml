<Window x:Class="AlgorithmLab1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AlgorithmLab1"
        
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1200">
<!-- -->
    <Window.DataContext>
        <local:ViewModel />
    </Window.DataContext>
   
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <StackPanel Grid.Row="0" VerticalAlignment="Center">
        <!--<lvc:CartesianChart
            Series="{Binding DataSeries}"
            x:Name="CartesianChart"
            ZoomMode="Both"
            Width="1200" Height="450"
        />  -->
            
            <lvc:CartesianChart 
                Series="{Binding Series}"
                x:Name="CartesianChart"
                ZoomMode="Both"
                Width="1200" Height="450"
            />
        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <Button x:Name="Run_btn" Width="200" Height="70" Click="Run_btn_Click" Content="Run it!" FontSize="36" />
            <Label x:Name="Label1" Content="0" FontSize="48"/>
            <Button Margin="6" Command="{Binding AddCommand}">Add</Button>
            <Button Margin="6" Command="{Binding UITestCommand}">Run UI Test</Button>

        </StackPanel>
    </Grid>
    
</Window>
